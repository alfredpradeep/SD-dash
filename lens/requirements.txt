# LENS — Linguistic Entropy Attribution Engine
# Python 3.11+
# Loose version pins for maximum compatibility

# ── Core API ──
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# ── Tokenization ──
tiktoken>=0.7.0

# ── Language Detection ──
# fasttext-wheel may fail on Python 3.13+; setup.sh handles this gracefully
fasttext-wheel>=0.9.2; python_version < "3.13"

# ── Entropy + ML ──
numpy>=1.26.0
scipy>=1.13.0
scikit-learn>=1.5.0

# ── Storage ──
clickhouse-connect>=0.7.0
redis>=5.0.0

# ── HTTP Client ──
aiohttp>=3.9.0
httpx>=0.27.0

# ── Utilities ──
loguru>=0.7.0
prometheus-client>=0.20.0

# ── Testing ──
pytest>=8.0.0
pytest-asyncio>=0.23.0
